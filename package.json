{
  "name": "nexusboard",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @nexusboard/work-management-service run build",
    "typecheck": "yarn workspace @nexusboard/work-management-service run typecheck",
    "format": "yarn workspace @nexusboard/work-management-service run format",
    "format:check": "yarn workspace @nexusboard/work-management-service run format:check",
    "start": "yarn workspace @nexusboard/work-management-service run start",
    "start:dev": "yarn workspace @nexusboard/work-management-service run start:dev",
    "start:debug": "yarn workspace @nexusboard/work-management-service run start:debug",
    "start:prod": "yarn workspace @nexusboard/work-management-service run start:prod",
    "lint": "yarn workspace @nexusboard/work-management-service run lint",
    "lint:fix": "yarn workspace @nexusboard/work-management-service run lint:fix",
    "test": "yarn workspace @nexusboard/work-management-service run test",
    "test:e2e": "yarn workspace @nexusboard/work-management-service run test:e2e",
    "check": "yarn workspace @nexusboard/work-management-service run check"
  }
}
